import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WallsHeightSmall here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WallsHeightSmall extends Walls
{
    /**
     * Act - do whatever the WallsHeightSmall wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public WallsHeightSmall()
    {
        getImage().scale(getImage().getWidth() / 4, getImage().getHeight() * 10);
    }
    
        public void act()
    {
        // Add your action code here.
    }
}
